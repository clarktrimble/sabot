## ginked from https://github.com/pkg/json 1/15/23
##
## agreeing with Mr. Cheney on disabled linters
## commented ones are mostly not generating hits
## Todo: how to best share b/t projects?

linters:
  enable-all: true
  disable:
    - asasalint
    #- bodyclose
    - canonicalheader ### why???
    - containedctx
    - contextcheck
    #- cyclop
    ###- deadcode # replaced with unused
    - depguard # too annoying
    #- dupl
    #- dupword
    - err113
    #- errcheck
    #- errchkjson
    #- errname
    #- errorlint
    ###- exhaustivestruct # Replaced by exhaustruct.
    - exhaustruct
    - exhaustive # too annoying, too hard to disable with comments
    - fatcontext ### want to be sarcastic, anyway should be ok in test
    - forcetypeassert # more stupid stuff
    #- forbidigo
    #- funlen
    - gci
    - gochecknoglobals
    #- gocognit
    #- goconst
    - gocritic
    #- gocyclo
    - godot
    - godox ## want Todo's today, pct
    ###- goerr113
    - gofumpt
    ###- golint # Replaced by revive.
    ###- gomnd
    ###- ifshort
    - inamedparam # too opinionated
    ###- interfacer # deprecated
    #- ireturn
    - lll
    #- maintidx
    ###- maligned # Replaced by govet
    - mnd
    - musttag # flaky, fires on structs which aren't JSON marshalled
    - nakedret # pct
    #- nilnil
    - nlreturn
    #- noctx
    - nonamedreturns
    ###- nosnakecase # Replaced by revive, pct
    - paralleltest
    - perfsprint ### more readable with fmt
    #- prealloc
    ###- scopelint # Replaced by exportloopref.
    ###- structcheck # replaced with unused
    - tagliatelle
    - tagalign # too fussy
    - tenv # deprecated horseshite, blah
    - testpackage # too annoying
    #- thelper
    - usestdlibvars
    ###- varcheck # Replaced by unused.
    - varnamelen
    - whitespace
    - wrapcheck
    - wsl
linters-settings:
  #gofumpt:
    #module-path: github.com/pkg/json
  revive:
    rules:
    - name: var-naming
      disabled: true
  stylecheck:
    checks: [
      "all",
      "-ST1003",
      "-ST1012"
    ]
  unparam:
    check-exported: false

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
